version = 1

[types]
concept = "Concepts"
doc = "Notes, docs, pages, specs"

[aliases]
requires = "depends_on"

[predicates.depends_on]
description = "A requires B."
domain = ["concept"]
severity = "error"

[vault]
unscoped = "allow"

[vault.scopes.people]
required = true
unmatched = "warn"
allow = ["**/*.md"]
